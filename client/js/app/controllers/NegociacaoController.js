class NegociacaoController {

  adiciona(event) {
    event.preventDefault();
    let $ = document.querySelector.bind(document);
    let inputQtd = $('#quantidade');
    let inputData = $('#data');
    let inputValor = $('#valor');
  }
}
